const router = require('express').Router()
const request = require('./request')
const URL = require('../config').couchdb.baseURL + 'tag/'

router.get('/:id', (req, res) => {

})
module.exports = router
