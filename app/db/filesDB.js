const router = require('express').Router()
const request = require('./request')
const URL = require('../config').couchdb.baseURL + 'assets_service/'

router.get('/:id', (req, res) => {

})
module.exports = router
